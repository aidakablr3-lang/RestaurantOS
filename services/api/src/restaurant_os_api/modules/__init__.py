"""Bounded-context business modules.

Each module owns its own domain/application/infrastructure/presentation
layers. Cross-module access is only permitted through a module's
``public`` package (see Technical Architecture v2.0 SS Group E).
"""
