# RestaurantOS v0.1.0-rc1 — Tenant Platform Release Candidate

**Branch:** `feature/tenant-platform-frontend` → `develop`
**HEAD at time of writing:** `a6aa9a0`
**Prepared:** 2026-08-07

No semantic version tags exist yet in this repository (tracked as a gap in
`docs/RELEASE_CHECKLIST.md`). `v0.1.0-rc1` is this release's working
identifier, not a git tag — tagging is a decision for whoever merges this
branch, not made unilaterally here.

---

## Executive Summary

This is the first release candidate for the **Tenant Platform** (Sprint
4.1) — RestaurantOS's first business platform, built on top of the Sprint
3 identity/auth foundation. It adds tenant lifecycle management (onboard,
suspend, reactivate, offboard), a platform-admin web UI to drive it, 84
backend tests, a 24-spec Playwright end-to-end suite, and the release
infrastructure (Docker Compose, CI, documentation) needed to make the
whole thing reproducible.

This RC1 pass specifically hardened the branch for merge: removed a
committed development private key, found and fixed a build-breaking bug
in the Docker image, found and fixed a CORS/port mismatch that would have
failed the entire CI end-to-end job on its first real run, and confirmed
no git remote exists yet to push to. All fixes are release-engineering
only — no application/business logic changed in this pass.

**Recommendation: ready to merge into `develop`, pending explicit
approval.** See [Merge Readiness](#merge-readiness) for the full
reasoning and residual risks.

---

## Version

`v0.1.0-rc1` (working identifier; no git tag cut). `main`/`develop` are
both still at `1747258`; this branch is 25 commits ahead. See
`docs/AI_HANDOFF.md` §5–§6 for exact commit boundaries.

---

## Features

**Backend (`services/api`, extends `modules/identity`):**
- Tenant lifecycle: onboard, get, list (paginated, status-filterable), update, suspend, reactivate, offboard
- `Subscription`, `SystemSetting`, `FeatureFlag`, `TenantDirectoryEntry` domain entities + self-service read endpoints (`/api/v1/tenants/me/*`)
- Platform-admin-gated REST API (`/api/v1/admin/tenants/*`), authorized via an interim `users.is_platform_admin` boolean
- `VerifyAccessTokenUseCase` + auth/tenant-validation middleware
- `TenantProvisioningService` orchestrating tenant creation

**Frontend (`apps/admin-web`, new Next.js 15 app):**
- Login, authenticated app shell, dark-mode toggle
- Tenant List (paginated, status filter, loading/error/empty states)
- Tenant Details, with Suspend/Reactivate behind a confirmation dialog
- Create Tenant, Edit Tenant

**Release infrastructure (this pass hardened, did not newly introduce):**
- `docker-compose.yml` + dev Dockerfile
- `scripts/export_openapi.py` + committed `docs/api/openapi.json`
- GitHub Actions CI: 5 jobs (lint, typecheck, backend tests, frontend build, E2E)
- `docs/DEVELOPMENT.md`, `.env.example` files, `docs/RELEASE_CHECKLIST.md`
- **New this pass:** locally-generated (not committed) dev JWT keypair workflow (`infrastructure/docker/dev-jwt/generate-dev-keys.sh` / `.py`)

---

## Fixes

Four fixes landed in this RC1 hardening pass, each its own commit:

1. **`7ff7a6f` — Stopped committing the dev JWT private key.** `infrastructure/docker/dev-jwt/{private,public}.pem` were tracked in git. Removed from the working tree, replaced with idempotent `generate-dev-keys.sh`/`.py` scripts that generate a local, gitignored keypair on demand. **Known limitation:** the removed files still exist in this branch's earlier commit history — this stops future commits, it does not rewrite history. See Known Limitations.

2. **`c1d4629` — Fixed a CORS/port mismatch that would have failed the entire `e2e` CI job.** `playwright.config.ts`'s `webServer` defaults to port 3100; the `e2e` job's backend was only configured to allow CORS from `localhost:3000`. Every browser request the Playwright suite made in CI would have been rejected by CORS before reaching the API — all 24 E2E tests would have failed the first time this job actually ran on GitHub. Fixed by pinning `E2E_PORT=3000` for that step. Also fixed: the CI-only `"github"` reporter wrote no report to disk, leaving the "Upload Playwright report" artifact step with nothing to upload; added the `html` reporter alongside it. Also corrected a stale doc claim in `apps/admin-web/e2e/README.md`.

3. **`b53ca56` — Fixed a build-breaking bug in the Dockerfile.** The image copied only `pyproject.toml` before running `pip install -e ".[dev]"`, intending to cache the dependency layer separately from source. This is wrong for a src-layout PEP 660 editable install: setuptools only maps files it can see at install time, so with no `src/` present yet, the editable install "succeeds" but the package is permanently unimportable — even after `src/` is copied in by a later layer. **Confirmed by reproducing the exact failure outside Docker**, then confirming the fix (copy source first, then install) resolves it. Since `alembic/env.py` imports `restaurant_os_api.core.config` at module load, this would have broken `docker compose up --build` immediately — not even migrations would have run. See [Docker Status](#docker-status).

4. **`a6aa9a0` — Documentation follow-up**, recording the Dockerfile fix in `docs/DEVELOPMENT.md`'s existing "not run against a live daemon" disclosure.

No fixes in this pass touched business logic, domain models, or the RLS/auth architecture.

---

## Breaking Changes

**None.** `develop`/`main` currently have no Tenant Platform code at all — this release is purely additive from `develop`'s perspective. No existing endpoint, schema, environment-variable name, or documented workflow on `develop` is altered or removed by this branch. (The dev-JWT-keypair workflow *did* change within this branch's own lifetime — see Migration Notes — but `develop` never had the old committed-keypair behavior to begin with.)

---

## Database Changes

**Migration `0002_tenant_platform.py`** (`services/api/alembic/versions/`), `down_revision = "0001"`, hand-written per Data Architecture v2.0 §7.1. Adds: `subscriptions`, `system_settings`, `feature_flags`, `tenant_directory_entries`, plus tenant lifecycle status transitions on the existing `tenants` table. RLS policies applied consistently with `0001`. Additive only — no destructive changes to Sprint 3 tables.

No new migrations were added in this RC1 hardening pass.

---

## Migration Notes

- Run `alembic upgrade head` from `services/api` after pulling this branch. No manual data backfill required.
- **New one-time step for anyone who already had this branch checked out:** run `./infrastructure/docker/dev-jwt/generate-dev-keys.sh` (or `.py`) before your next `docker compose up` — the previously-committed keypair no longer exists in the working tree. `docker-compose.yml`, `docs/DEVELOPMENT.md`, and the root `README.md` were all updated to describe this.
- If you have a local checkout with the *old* committed `private.pem`/`public.pem` still present on disk from before this branch's history, those files are now untracked (not deleted from your working tree by `git pull`/`git merge` unless you also discard local changes) — safe to leave or delete either way, since `.gitignore` now excludes them going forward.
- The `SET LOCAL` → `set_config()` fix from Step 3 (`07dea29`) applies to every module's `UnitOfWork`, not just the Tenant Platform's — carried forward unchanged in this RC.

---

## Testing Summary

Full suite re-run immediately before this report was written (not carried over from earlier in the session):

| Suite | Result |
|---|---|
| Backend unit tests | 49/49 passing |
| Backend integration tests (incl. 17 admin-router, 2 `set_config()` regression) | 84/84 passing (full suite) |
| `ruff format --check` / `ruff check` | Clean |
| `mypy src/` | 5 pre-existing findings (unchanged from prior sessions — advisory, non-blocking in CI) |
| Frontend `tsc --noEmit` | Clean |
| Frontend `eslint .` | Clean |
| Frontend `next build` | Clean |
| Frontend E2E (Playwright, 24 specs) | Not re-run live this pass (see note below) — last confirmed 24/24 passing earlier this session, app/spec code unchanged since |

**Why the E2E suite wasn't re-run live in this pass:** this pass's only Playwright-adjacent changes (`E2E_PORT` pin, CI-only `html` reporter) are both gated behind `process.env.CI`/`E2E_PORT`, neither of which is set in a normal local `npm run e2e` run — so local behavior is provably unaffected by either change. A stale, unrelated dev-server process was already occupying port 3100 in this environment when this check would have run; rather than force a re-run through it, this report relies on reasoning about the gated config plus the suite's already-confirmed result.

---

## Release Metrics

- **34 commits** total on `feature/tenant-platform-frontend`; **25 ahead** of `develop`/`main` (which remain at `1747258`)
- **84 backend tests**, **24 frontend E2E specs** = 108 automated tests covering this release
- **6 of 10** originally-scoped admin-web screens shipped (4 explicitly deferred, user-approved)
- **4 real defects** found and fixed via real-backend/browser verification in Step 3 (backend `SET LOCAL` bug, missing CORS, 3 frontend issues counted together as one commit)
- **1 real defect** found via E2E testing in Step 4 (session-cleared-on-unrelated-401)
- **2 real defects** found via this RC1 static/reproduction-based review (Dockerfile build-order, CI CORS/port mismatch) — neither could have been caught without actually reproducing the failure, since neither Docker nor a GitHub remote exist in this environment
- **1 security hardening item** (removed committed dev private key)

---

## Known Limitations

- **4 of 10 originally-scoped admin-web screens deferred**: Subscription Status, Quota Dashboard, Feature Flag Display, Tenant Settings. Blocked on admin-scoped backend routes that don't exist yet. User-approved scope-down, not a defect.
- **`GET /admin/tenants/{unknown-id}` returns 401, not 404** — reuses the auth-time `TenantNotFoundError`. Documented, not fixed.
- **The old committed dev JWT keypair still exists in this branch's earlier commit history.** This RC stops committing it going forward; it does not rewrite history to purge it retroactively. Practical exposure is low (the key never protected anything but a developer's own disposable local Postgres instance), but a full purge (`git filter-repo`/BFG) is a deliberate decision for whoever owns this repository, not a side effect of this release.
- **5 pre-existing `mypy` findings** (`OutboxWriter.publish` argument-type mismatches) — predate this sprint, `backend-typecheck` CI job intentionally non-blocking until addressed.
- A platform-admin user in the same tenant it suspends will suspend its own session too (suspending a tenant correctly revokes all of that tenant's sessions) — operational note, not a bug.
- No test-coverage percentage tooling configured; coverage is qualitative (see `docs/AI_HANDOFF.md`).
- No production-shaped Docker image, staging/production deployment pipeline, or CI-enforced architecture-boundary linting — tracked in `docs/RELEASE_CHECKLIST.md`'s "Not yet in place" section as deliberate, out-of-scope gaps.

---

## Docker Status

**Docker is not installed in this environment** (`docker`/`docker compose` both resolve to "command not found") — `docker compose up --build` has never actually been executed here. Per this RC's own instructions, that is disclosed plainly rather than claimed.

What *was* done in lieu of execution:
- Full static review of `docker-compose.yml` and `infrastructure/docker/api/Dockerfile`: env-var wiring cross-checked line-by-line against `services/api/src/restaurant_os_api/core/config.py`'s actual `Settings` field names/prefixes (`DATABASE_*`, `JWT_*`, `CORS_ALLOWED_ORIGINS`, `APP_ENV`) — all consistent.
- YAML syntax validated with a parser (not just eyeballed).
- `.dockerignore` reviewed — excludes `.venv`, caches, `.env*`, `*.pem`, `.git/`; no secret-leak risk into the build context.
- **One real, build-breaking bug found and fixed** (see Fixes #3): the Dockerfile's dependency-layer-caching COPY order made the built package permanently unimportable. Found and confirmed by *actually reproducing* the exact `pip install -e .` sequence outside Docker (not by inspection alone) — this is the strongest verification possible without a Docker daemon, but it is still not the same as a real `docker compose up --build`.
- Alembic's `env.py` import chain traced to confirm the above bug would have failed migrations immediately, not just at runtime import.

**What remains genuinely unverified:** the full `docker compose up --build` sequence — Postgres startup, container networking, the bind-mount interaction with the now-corrected image, and all of Login/Tenant List/Create Tenant running through an actual containerized backend. This is the single most important outstanding action item before this RC can be called fully verified.

---

## GitHub Actions Status

**No git remote is configured in this repository** (`git remote -v` returns nothing; no branch tracks an upstream) — `.github/workflows/ci.yml` has never executed on GitHub's infrastructure. Disclosed rather than claimed.

What *was* done in lieu of execution:
- Full workflow review against 6 explicit criteria (syntax, job dependencies, matrix usage, service containers, artifact paths, caching, triggers) — see `docs/AI_HANDOFF.md` for the itemized table.
- YAML syntax validated with a parser.
- **One real, job-failing bug found and fixed**: the `e2e` job's CORS/port mismatch (Fixes #2) — traced by reading `playwright.config.ts`'s actual default port logic against the backend's hardcoded `CORS_ALLOWED_ORIGINS`, not assumed.
- One artifact-upload bug found and fixed: `reporter: "github"` produces no report file for the existing "Upload Playwright report" step to find.
- Two CI-quality improvements added: pip caching on all 4 Python jobs, a `concurrency` group to cancel superseded runs.

**What remains genuinely unverified:** a real run on GitHub's runners. Every fix above was verified by tracing actual config/env-var values and, where possible, reproducing the specific failure mode locally — not by observing a real green (or red) run.

---

## Merge Readiness

**Recommend approval to merge `feature/tenant-platform-frontend` → `develop`.**

Reasoning:
- Every runtime application change on this branch is additive relative to `develop` (which has none of this yet) or a pre-approved critical-bug fix — nothing on `develop` today is altered or removed.
- 84 backend tests and 24 E2E tests are real, reproducible, and green (backend suite re-confirmed fresh for this report).
- This RC1 pass found and fixed two release-engineering bugs (Docker build order, CI CORS/port mismatch) that would otherwise have surfaced as confusing failures on someone's *first* real attempt to use this infrastructure — finding them now, via careful static work, is strictly better than finding them live.
- The two things that remain genuinely unverified (a live `docker compose up --build`, a live GitHub Actions run) are infrastructure, not application code. If either is still broken in some way this session's reproduction-based approach couldn't catch, it fails loudly and obviously (a build error, a red CI run) — it does not silently corrupt data or break anything already working on `develop`.
- The dev-JWT-key history exposure is real but low-severity (the key protected nothing but disposable local dev data) and explicitly disclosed, not hidden.

**This report is informational. It does not constitute the merge itself** — per this session's instructions, `git merge` will not run without explicit approval in the same turn, and nothing will be pushed to any remote (none exists to push to).

---

## Next Sprint

Not decided by this release — still an open choice between:
1. Closing the Tenant Platform's remaining gap (Subscription/Quota/Feature-Flag/Settings admin screens + backend routes).
2. Beginning the Restaurant Platform (explicitly deferred by the user until Tenant Platform work is settled).

Also outstanding regardless of which is chosen: get real confirmation of `docker compose up --build` and a real GitHub Actions run once Docker and a remote are available — both are now believed correct based on careful static work, but "believed correct" and "confirmed" are different claims, and this report is careful to keep them separate.
